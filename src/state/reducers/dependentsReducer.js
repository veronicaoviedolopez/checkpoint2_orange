
const initialState =  {
    DependentsList : [],
}

const Fetch_Dependents = (state = initialState, action )=>{
    switch (action.type){
        default:
            return state
    }
}

export default dependentsReducer;